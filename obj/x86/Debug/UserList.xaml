<Page
    x:Class="TestCodeMindX.UserList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestCodeMindX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local1="using:TestCodeMindX.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource SystemControlAcrylicWindowBrush}">
        <TextBox x:ConnectionId='2' BorderThickness="1" x:Name="filter" PlaceholderText="Filter"                                   Margin="5"></TextBox>
        <RelativePanel>
            <ListView x:ConnectionId='6' Name="InventoryList"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.IsVerticalRailEnabled="True"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.IsHorizontalRailEnabled="True"
                  Margin="50">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal"  >
                            <TextBlock Text="userName" Width="150" Foreground="DarkRed" HorizontalAlignment="Center"/>
                            <TextBlock Text="fullName" Width="150" Foreground="DarkRed" HorizontalAlignment="Center" />
                            <TextBlock Text="email" Width="200" Foreground="DarkRed" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate                         >
                        <StackPanel x:ConnectionId='9' Orientation="Horizontal" >
                            <TextBlock x:ConnectionId='10' Name="ItemName"
                                                            
                                    Width="150" />
                            <TextBlock x:ConnectionId='11'                         
                                   Width="150" />
                            <TextBlock x:ConnectionId='12'                      
                                   Width="200" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </RelativePanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="10">
            <Button x:ConnectionId='3'                             >Previous</Button>
            <Button x:ConnectionId='4'                         >Next</Button>
            <Button x:ConnectionId='5'                       HorizontalAlignment="Right">Sign Out</Button>
        </StackPanel>
    </Grid>
</Page>

